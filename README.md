# ecommerce-store-nextjs

This repository includes a bash script to bootstrap a Next.js project with TypeScript.

Run `./setup-next-ts.sh` to create the `ecommerce-store-nextjs` directory, install the required dependencies, and generate a `tsconfig.json` configured for Next.js.
